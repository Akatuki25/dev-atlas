// Code generated by mss-protoc-gen. DO NOT EDIT.

"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { listWorkLogs } from "../../client/work_log";
import type { WorkLog } from "../../types/work_log";

// 優先度カードリスト(全列テーブルにしない): primary を突出、secondary を従属。
export function WorkLogList() {
  const [rows, setRows] = useState<WorkLog[]>([]);
  useEffect(() => { listWorkLogs().then(setRows).catch(() => setRows([])); }, []);
  return (
    <div>
      <Link href="/work_logs/new">+ New</Link>
      <ul style={{ listStyle: "none", padding: 0, display: "grid", gap: 8, marginTop: 12 }}>
        {rows.map((r) => (
          <li key={r.id} style={{ border: "1px solid #ddd", borderRadius: 8, padding: 12 }}>
            <Link href={`/work_logs/${r.id}`} style={{ textDecoration: "none", color: "inherit" }}>
              <div style={{ fontWeight: 700, fontSize: 16 }}>{r.project_id}</div>
              <div style={{ color: "#666", fontSize: 13 }}>{r.id} <span> · </span> {r.minutes} <span> · </span> {new Date(r.created_at_unix * 1000).toLocaleDateString()}</div>
            </Link>
          </li>
        ))}
      </ul>
    </div>
  );
}
