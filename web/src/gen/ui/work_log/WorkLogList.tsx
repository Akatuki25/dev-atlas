// Code generated by mss-protoc-gen. DO NOT EDIT.

"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { listWorkLogs } from "../../client/work_log";
import type { WorkLog } from "../../types/work_log";
import { ListStack, ListRow, EmptyState } from "../../../lib/widgets";

// 優先度カードリスト(全列テーブルにしない): primary を突出、secondary を従属。見た目は widget に委譲。
export function WorkLogList() {
  const [rows, setRows] = useState<WorkLog[]>([]);
  useEffect(() => { listWorkLogs().then(setRows).catch(() => setRows([])); }, []);
  if (rows.length === 0) {
    return <EmptyState message="No work_logs yet" action={<Link href="/work_logs/new">+ New</Link>} />;
  }
  return (
    <ListStack>
      {rows.map((r) => (
        <ListRow key={r.id} href={`/work_logs/${r.id}`} primary={<>{r.project_id}</>} secondary={<>{r.id} <span> · </span> {r.minutes} <span> · </span> {new Date(r.created_at_unix * 1000).toLocaleDateString()}</>} />
      ))}
    </ListStack>
  );
}
